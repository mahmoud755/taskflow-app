# Reddit DevOps Project

This repository contains a Reddit clone application containerized with Docker and will be deployed using Kubernetes, Terraform, Ansible, GitHub Actions, AWS, Prometheus, Grafana, and EFK.

## Running Locally with Docker

1. Build and start the Reddit clone:

```bash
docker compose up -d --build

